import React from "react"

const Loading = () => {
    return(
        <div id="loading" style={{ height: "100vh", width: "100%", backgroundColor: "#12242A", display: "flex", justifyContent: "center", alignItems: "center" }}>
            <h1 style={{ color: "#21AEA3" }}>Loading...</h1>
        </div>
    )
}

export default Loading 